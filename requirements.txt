# NOTE: Install PyTorch separately per https://pytorch.org/get-started/locally
# Choose the correct command for your platform (macOS MPS, Linux/Windows CUDA, etc.)

# Core libraries used by `playground.py`
transformers>=4.34.0
accelerate>=0.18.0
datasets>=2.12.0
peft>=0.4.0
trl>=0.5.0
safetensors>=0.3.0
sentencepiece>=0.1.98
huggingface-hub>=0.13.0

# Optional / performance-related (may be platform-specific)
bitsandbytes>=0.39.0
# xformers can improve attention performance; install only if supported on your platform
# xformers==0.0.20

# If you want the bleeding-edge `trl` fixes, install from git (optional):
# git+https://github.com/huggingface/trl.git@main
